let idade = 1

do{
    console.log('Passou');
    idade = idade + 1;
} while(idade < 18);

/*
 declaração do{} while(), cria um laço que executa uma declaração até que o teste da condição for falsa (false).
 A condição é avaliada depois que o bloco de código é executado, resultando que uma declaração seja executada pelo menos uma vez. */